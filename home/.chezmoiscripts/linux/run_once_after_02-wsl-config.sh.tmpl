#!/usr/bin/env bash
set -euo pipefail

{{ if and .have_root .is_wsl -}}

sudo tee /etc/wsl.conf > /dev/null <<'EOF'
[boot]
systemd=true

[automount]
enabled = true
options = "metadata"
mountFsTab = true

[network]
generateResolvConf = false
EOF

{{ end -}}
